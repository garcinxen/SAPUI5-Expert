<mvc:View controllerName="logaligroup.invoices.controller.InvoicesList"
          xmlns="sap.m"
          xmlns:mvc="sap.ui.core.mvc">
    <List id="invoiceList"
          headerText="{i18n>invoiceListTitle}"
          class="sapUiResponsiveMArgin"
          width="auto"
          items="{path: 'northwind>/Invoices',
                  sorter: {path: 'ShipperName',
                           group: true }
                 }">

        <headerToolbar>
            <Toolbar id="invoicesToolbar">
                <Title id="titleInvoices"
                       text="{i18n>invoiceListTitle}" />
                <ToolbarSpacer id="spacer" />
                <SearchField id="searchInvoice"
                             width="50%"
                             search=".onFilterInvoice" />
            </Toolbar>
        </headerToolbar>
        <items>
        <!-- Para quitar el redondeo añadir roundingMode: 'TOWARDS_ZERO' en el formatOptions -->
            <ObjectListItem id="items" 
                            title="{northwind>Quantity} x {northwind>ProductName}"
                            number="{
                                parts: [{path: 'northwind>ExtendedPrice'},
                                        {path: 'currencyUnit>/usd'}],
                                type:'sap.ui.model.type.Currency',
                                formatOptions: {
                                    showMeasure: false,
                                    decimals: 2,
                                    style: 'short'                                    
                                }
                            }"
                            numberUnit="{currencyUnit>/usd}"
                            numberState="{= ${northwind>ExtendedPrice} > 50 ? 'Success' : 'Error'}">
            </ObjectListItem>
        </items>
    </List>
</mvc:View>